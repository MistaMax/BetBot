/*jest.setTimeout(2);
it("Intentional Fail"){
    const bic = "hell"
    expect(bic).toEqual(null);
}*/
it("Test addition", () => {//just a simple test
    const a = 1+1;
    expect(a).toEqual(2);
});